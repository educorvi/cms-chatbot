#$schema: https://raw.githubusercontent.com/educorvi/cms-chatbot/main/conf.schema.json
$schema: ./conf.schema.json
open_ai:
  api_key: ""
  model: gpt-3.5-turbo-16k
  spending_limit: 50

search:
  typesense:
    host:
    port:
    protocol:
    collection:
    result_number: 4
    result_size: 300
    api_key:
  elasticsearch:
    url:
    index:
    result_number: 0
    result_size: 0
  engine: typesense

websocket:
  port: 8765

deepl:
  enabled: true

# Replaces parts of the source url that is returned to the client
source_replace:
  # Supports regex
  from: ""
  to: ""
